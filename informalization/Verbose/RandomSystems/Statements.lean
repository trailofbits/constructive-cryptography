import Verbose.RandomSystems.Notation

/-!
# Retired statement-notation prototype

This compatibility module intentionally exports no additional syntax. The old
switching-specific `Delta`/`Δ` notation elaborated to
`PDS.advFullyDefined`, which is a different quantity. Standalone `URF`, `URP`,
query restriction, and `Adv⊥` notation now lives in
`Verbose.RandomSystems.Notation`.
-/
